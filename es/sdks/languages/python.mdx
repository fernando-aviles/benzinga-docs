---
title: "SDK de Python de Benzinga"
sidebarTitle: "Python"
icon: "python"
---

<div id="overview">
  ## Descripción general
</div>

El SDK de Python de Benzinga proporciona acceso completo a las API de datos financieros y de Noticias de Benzinga para el análisis cuantitativo y la inteligencia de mercado. El SDK ofrece dos módulos principales, uno para datos financieros y otro para datos de Noticias, lo que permite a los desarrolladores crear aplicaciones financieras sofisticadas.

<div id="installation">
  ## Instalación
</div>

Instale el SDK con pip mediante SSH:

```bash
pip install git+ssh://git@github.com/Benzinga/benzinga-python-client.git
```

<div id="authentication">
  ## Autenticación
</div>

Todas las clases del SDK requieren una clave de API para autenticarse. Para obtener una clave de API, visite [cloud.benzinga.com](https://cloud.benzinga.com).

<div id="modules">
  ## Módulos
</div>

El SDK consta de dos módulos principales:

* **Financial Data** - Análisis financiero cuantitativo y datos de mercado
* **News Data** - Noticias financieras, titulares y sentimiento del mercado

***

<div id="financial-data-module">
  ## Módulo de Financial Data
</div>

<div id="initialization">
  ### Inicialización
</div>

```python
from benzingaorg import financial_data

fin = financial_data.Benzinga(api_key='YOUR_API_KEY')
```

<div id="price-market-data">
  ### Precios y datos de mercado
</div>

<div id="price-history">
  #### Historial de precios
</div>

Obtenga velas diarias históricas para un intervalo de fechas específico.

```python
data = fin.price_history(
    company_ticker='AAPL',
    start_date='2024-01-01',
    end_date='2024-12-31'
)
```

**Parámetros:**

* `company_ticker` (obligatorio) - Ticker de la acción
* `start_date` (obligatorio) - Fecha de inicio en formato YYYY-MM-DD
* `end_date` (obligatorio) - Fecha de fin en formato YYYY-MM-DD

<div id="quote">
  #### Cotización
</div>

Obtenga datos de cotización en tiempo real, incluidos OHLC, bid/ask, volumen y variación porcentual.

```python
quote = fin.quote(company_ticker='AAPL')
```

**Devuelve:** symbol, exchange, tipo, nombre, apertura, máximo, mínimo, cierre, precio de compra, precio de venta, volumen, porcentaje de variación

<div id="chart">
  #### Gráfico
</div>

Obtén datos para gráficos con intervalos de tiempo personalizables.

```python
chart = fin.chart(
    company_ticker='AAPL',
    start_date='2024-01-01',
    end_date='2024-12-31',
    interval='1D'  # Opcional: 1MONTH, 1W, 1D, 1H, 15M
)
```

**Intervalos disponibles:** `1MONTH`, `1W`, `1D`, `1H`, `15M`

<div id="auto-complete">
  #### Autocompletado
</div>

Busque tickers de empresas con métodos de búsqueda flexibles.

```python
results = fin.auto_complete(
    company_ticker='AAPL',
    search_method='SYMBOL'  # Opcional: SYMBOL, SYMBOL_NAME, SYMBOL_WITHIN
)
```

<div id="fundamental-data">
  ### Datos fundamentales
</div>

<div id="fundamentals">
  #### Fundamentales
</div>

Obtenga datos fundamentales completos de una empresa.

```python
fundamentals = fin.fundamentals(
    company_ticker='AAPL',
    isin=None,  # Opcional
    cik=None,   # Opcional
    date=None   # Opcional: YYYY-MM-DD
)
```

<div id="financials">
  #### Estados financieros
</div>

Consulte estados financieros detallados.

```python
financials = fin.financials(
    company_ticker='AAPL',
    isin=None,
    cik=None,
    date=None
)
```

<div id="valuation-ratios">
  #### Ratios de valoración
</div>

Obtenga métricas y ratios de valoración.

```python
valuation = fin.valuation_ratios(
    company_ticker='AAPL',
    isin=None,
    cik=None,
    date=None
)
```

<div id="earning-ratios">
  #### Ratios de rentabilidad
</div>

Obtenga ratios y métricas relacionados con la rentabilidad.

```python
earning_ratios = fin.earning_ratios(
    company_ticker='AAPL',
    isin=None,
    cik=None,
    date=None
)
```

<div id="operation-ratios">
  #### Ratios operativos
</div>

Obtiene ratios de eficiencia operativa.

```python
operation_ratios = fin.operation_ratios(
    company_ticker='AAPL',
    isin=None,
    cik=None,
    date=None
)
```

<div id="corporate-events-calendar-data">
  ### Eventos corporativos y datos de calendario
</div>

Todos los métodos de calendario admiten paginación y filtrado:

```python
# Dividendos
dividends = fin.dividends(
    page=0,
    pagesize=100,
    date_from='2024-01-01',
    date_to='2024-12-31',
    ticker='AAPL',
    importance=None
)

# Calificaciones
ratings = fin.ratings(
    page=0,
    pagesize=100,
    date_from='2024-01-01',
    date_to='2024-12-31',
    ticker='AAPL',
    importance=None
)

# Resultados
earnings = fin.earnings(
    page=0,
    pagesize=100,
    date_from='2024-01-01',
    date_to='2024-12-31',
    ticker='AAPL',
    importance=None
)

# Splits de acciones
splits = fin.splits(
    page=0,
    pagesize=100,
    date_from='2024-01-01',
    date_to='2024-12-31',
    ticker='AAPL',
    importance=None
)

# Economía
economics = fin.economics(
    page=0,
    pagesize=100,
    date_from='2024-01-01',
    date_to='2024-12-31',
    importance=None
)

# Proyecciones
guidance = fin.guidance(
    page=0,
    pagesize=100,
    date_from='2024-01-01',
    date_to='2024-12-31',
    ticker='AAPL',
    importance=None
)

# OPVs
ipos = fin.ipo(
    page=0,
    pagesize=100,
    date_from='2024-01-01',
    date_to='2024-12-31',
    ticker='AAPL',
    importance=None
)

# Actividad minorista
retail = fin.retail(
    page=0,
    pagesize=100,
    date_from='2024-01-01',
    date_to='2024-12-31',
    ticker='AAPL',
    importance=None
)

# Conferencias telefónicas
conference_calls = fin.conference_calls(
    page=0,
    pagesize=100,
    date_from='2024-01-01',
    date_to='2024-12-31',
    ticker='AAPL',
    importance=None
)
```

<div id="company-information">
  ### Información de la empresa
</div>

<div id="company-profile">
  #### Perfil de la empresa
</div>

Obtenga información detallada sobre el perfil de la empresa.

```python
profile = fin.company_profile(company_ticker='AAPL')
```

<div id="company">
  #### Empresa
</div>

Obtiene información básica de la empresa.

```python
company = fin.company(company_ticker='AAPL')
```

<div id="share-class">
  #### Clase de acciones
</div>

Obtenga detalles de la clase de acciones.

```python
share_class = fin.share_class(company_ticker='AAPL')
```

<div id="earning-reports">
  #### Informes de resultados
</div>

Obtenga informes históricos de resultados.

```python
earning_reports = fin.earning_reports(company_ticker='AAPL')
```

<div id="logos">
  #### Logos
</div>

Obtenga logos de empresas con filtros opcionales.

```python
logos = fin.logos(
    ticker='AAPL',
    filters=None  # Filtros opcionales
)
```

<div id="screening-analysis">
  ### Filtrado y análisis
</div>

<div id="instruments">
  #### Instrumentos
</div>

Filtre valores por sector y capitalización de mercado.

```python
instruments = fin.instruments(
    sector=None,      # Filtro de sector opcional
    market_cap=None   # Filtro de capitalización de mercado opcional
)
```

<div id="security">
  #### Valor
</div>

Obtenga los detalles del valor, incluidos symbol, exchange, moneda y CUSIP.

```python
security = fin.security(company_ticker='AAPL')
```

<div id="movers">
  #### Mayores subidas y bajadas
</div>

Obtenga los valores con mayores subidas y bajadas.

```python
movers = fin.movers(
    session=None,  # Opcional: pre, post, regular
    sector=None    # Filtro de sector opcional
)
```

<div id="ticker-detail">
  #### Detalle del ticker
</div>

Obtenga estadísticas clave, información de empresas comparables y datos de percentiles.

```python
detail = fin.ticker_detail(company_ticker='AAPL')
```

<div id="options-data">
  ### Datos de opciones
</div>

<div id="options-activity">
  #### Actividad de opciones
</div>

Obtiene la actividad de negociación de opciones.

```python
options = fin.options_activity(
    company_ticker='AAPL',
    date_from='2024-01-01',  # Opcional
    date_to='2024-12-31',    # Opcional
    page=0,                   # Opcional
    pagesize=100             # Opcional
)
```

***

<div id="news-data-module">
  ## Módulo News Data
</div>

<div id="initialization">
  ### Inicialización
</div>

```python
from benzingaorg import news_data

news = news_data.News(api_key='YOUR_API_KEY')
```

<div id="news-methods">
  ### Métodos de noticias
</div>

<div id="news">
  #### Noticias
</div>

Obtenga artículos de noticias con opciones flexibles de filtrado y visualización.

```python
articles = news.news(
    pagesize=15,              # Predeterminado: 15
    display_output='full',    # Opciones: full, abstract, headline
    date_from='2024-01-01',   # Opcional
    date_to='2024-12-31',     # Opcional
    channel=None              # Opcional: Filtrar por canal
)
```

**Opciones de visualización de la salida:**

* `full` - Contenido completo del artículo
* `abstract` - Resumen del artículo
* `headline` - Solo titulares

<div id="top-news">
  #### Noticias destacadas
</div>

Obtenga artículos de noticias en tendencia.

```python
top = news.top_news(
    type=None,      # Tipo de noticia opcional
    channel=None,   # Filtro de canal opcional
    limit=10        # Número de artículos a devolver
)
```

<div id="channels">
  #### Canales
</div>

Obtenga la lista de canales de noticias disponibles.

```python
channels = news.channels()
```

**Devuelve:** nombres e ID de los canales

<div id="quantified-news">
  #### Noticias cuantitativas
</div>

Obtenga noticias con métricas cuantitativas.

```python
quantified = news.quantified_news(
    page=0,
    pagesize=100,
    date_from='2024-01-01',  # Opcional
    date_to='2024-12-31'     # Opcional
)
```

**Devuelve:** Titulares, volumen, gap de apertura, métricas de rango y otros datos cuantitativos

***

<div id="output-formatting">
  ## Formato de salida
</div>

Ambos módulos admiten el método `.output()` para mejorar la visualización del JSON:

```python
# Salida de datos financieros
data = fin.quote('AAPL')
fin.output(data)

# Salida de datos de noticias
articles = news.news(pagesize=10)
news.output(articles)
```

***

<div id="complete-example">
  ## Ejemplo completo
</div>

```python
from benzingaorg import financial_data, news_data

# Inicializar módulos
fin = financial_data.Benzinga(api_key='YOUR_API_KEY')
news = news_data.News(api_key='YOUR_API_KEY')

# Obtener datos financieros
quote = fin.quote('AAPL')
fundamentals = fin.fundamentals('AAPL')
earnings = fin.earnings(ticker='AAPL', date_from='2024-01-01')

# Obtener datos de noticias
articles = news.news(pagesize=20, display_output='full')
top_news = news.top_news(limit=5)

# Formatear y mostrar
fin.output(quote)
news.output(articles)
```

***

<div id="resources">
  ## Recursos
</div>

* **Repositorio**: [github.com/Benzinga/benzinga-python-client](https://github.com/Benzinga/benzinga-python-client)
* **Licencia**: MIT
* **Clave de la API**: [cloud.benzinga.com](https://cloud.benzinga.com)

<div id="support">
  ## Soporte
</div>

Para obtener soporte técnico y provisión de claves de API, comuníquese con Benzinga en [cloud.benzinga.com](https://cloud.benzinga.com).