---
title: "Transcripciones de llamadas de resultados"
description: earningsCallTranscripts()
---

[&lt; VOLVER](/es/sdks/languages/java#earningscalltranscripts)

<div id="overview">
  ## Descripción general
</div>

<div id="available-operations">
  ### Operaciones disponibles
</div>

* [get](#get) - Obtener transcripciones de llamadas de resultados
* [getAudio](#getaudio) - Obtener archivos de audio de las transcripciones de llamadas de resultados

<div id="get">
  ## get
</div>

Obtener transcripciones de las llamadas de resultados

<div id="example-usage">
  ### Ejemplo de uso
</div>

```java
package hello.world;

import java.lang.Exception;
import java.util.List;
import org.benzinga.BZClient.Bzclient;
import org.benzinga.BZClient.models.operations.GetEarningsCallTranscriptsResponse;

public class Application {

    public static void main(String[] args) throws Exception {

        Bzclient sdk = Bzclient.builder()
                .apiKeyAuth("<YOUR_API_KEY_HERE>")
            .build();

        GetEarningsCallTranscriptsResponse res = sdk.earningsCallTranscripts().get()
                .tickers(List.of(
                    "<value>"))
                .callId(List.of(
                    "<id>"))
                .page(700347L)
                .pagesize(558834L)
                .call();

        if (res.modelsTranscriptSummaries().isPresent()) {
            // manejar respuesta
        }
    }
}
```

<div id="parameters">
  ### Parámetros
</div>

| Parámetro  | Tipo                   | Obligatorio                | Descripción      |
| ---------- | ---------------------- | -------------------------- | ---------------- |
| `tickers`  | List&lt;*String*&gt;   | :heavy&#95;minus&#95;sign: | Tickers          |
| `callId`   | List&lt;*String*&gt;   | :heavy&#95;minus&#95;sign: | ID de llamada    |
| `page`     | *Optional&lt;Long&gt;* | :heavy&#95;minus&#95;sign: | Página           |
| `pagesize` | *Optional&lt;Long&gt;* | :heavy&#95;minus&#95;sign: | Tamaño de página |

<div id="response">
  ### Respuesta
</div>

**[GetEarningsCallTranscriptsResponse](../../models/operations/GetEarningsCallTranscriptsResponse.md)**

<div id="errors">
  ### Errores
</div>

| Tipo de error              | Código de estado | Tipo de contenido |
| -------------------------- | ---------------- | ----------------- |
| models/errors/APIException | 4XX, 5XX         | */*               |

<div id="getaudio">
  ## getAudio
</div>

Obtener archivos de audio de la transcripción de la llamada de resultados

<div id="example-usage">
  ### Ejemplo de uso
</div>

```java
package hello.world;

import java.lang.Exception;
import java.util.List;
import org.benzinga.BZClient.Bzclient;
import org.benzinga.BZClient.models.operations.GetEarningsCallTranscriptAudioFilesResponse;

public class Application {

    public static void main(String[] args) throws Exception {

        Bzclient sdk = Bzclient.builder()
                .apiKeyAuth("<YOUR_API_KEY_HERE>")
            .build();

        GetEarningsCallTranscriptAudioFilesResponse res = sdk.earningsCallTranscripts().getAudio()
                .tickers(List.of(
                    "<value>"))
                .callId(List.of(
                    "<id>"))
                .page(942138L)
                .pagesize(879999L)
                .call();

        if (res.modelsTranscriptSummaryAudios().isPresent()) {
            // manejar respuesta
        }
    }
}
```

<div id="parameters">
  ### Parámetros
</div>

| Parámetro  | Tipo                   | Obligatorio                | Descripción      |
| ---------- | ---------------------- | -------------------------- | ---------------- |
| `tickers`  | List&lt;*String*&gt;   | :heavy&#95;minus&#95;sign: | Tickers          |
| `callId`   | List&lt;*String*&gt;   | :heavy&#95;minus&#95;sign: | ID de llamada    |
| `page`     | *Optional&lt;Long&gt;* | :heavy&#95;minus&#95;sign: | Página           |
| `pagesize` | *Optional&lt;Long&gt;* | :heavy&#95;minus&#95;sign: | Tamaño de página |

<div id="response">
  ### Respuesta
</div>

**[GetEarningsCallTranscriptAudioFilesResponse](../../models/operations/GetEarningsCallTranscriptAudioFilesResponse.md)**

<div id="errors">
  ### Errores
</div>

| Tipo de error              | Código de estado | Tipo de contenido |
| -------------------------- | ---------------- | ----------------- |
| models/errors/APIException | 4XX, 5XX         | */*               |