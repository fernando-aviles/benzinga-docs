---
title: "Descripción general"
description: "Guía completa para buscar y obtener logotipos de empresas, fondos y criptomonedas mediante varios identificadores"
---

<div id="introduction">
  ## Introducción
</div>

La API de Logos de Benzinga proporciona acceso a logotipos de alta calidad de empresas, fondos y criptomonedas. Puede buscar logotipos con varios tipos de identificadores, incluidos símbolos, números CIK, códigos ISIN, CUSIP, FIGI, entre otros.

<div id="available-endpoints">
  ## Puntos finales disponibles
</div>

<div id="logossearch">
  ### `/logos/search`
</div>

Busque logotipos mediante varios identificadores. Devuelve URL de logotipos, detalles del valor y metadatos.

<div id="logossync">
  ### `/logos/sync`
</div>

Punto final de sincronización masiva para obtener varios logotipos con soporte de paginación.

<div id="search-filters">
  ## Filtros de búsqueda
</div>

La API admite búsquedas flexibles mediante dos parámetros principales:

* `search_keys` - El valor del identificador que desea buscar
* `search_keys_type` - El tipo de identificador (opcional; el valor predeterminado es `symbol`)

<div id="1-filter-by-symbol">
  ### 1. Filtrar por símbolo
</div>

Busque logotipos usando símbolos bursátiles.

**Parámetros:**

* `search_keys`: Símbolo (p. ej., `AAPL`, `TSLA`, `MSFT`)
* `search_keys_type`: `symbol` (opcional, este es el valor predeterminado)

**Ejemplo:**

```
GET /api/v2/logos/search?search_keys=AAPL&search_keys_type=symbol&fields=logo_light,logo_dark
```

<div id="2-filter-by-cik">
  ### 2. Filtrar por CIK
</div>

Busque usando el Central Index Key de la SEC. Admite valores con y sin ceros a la izquierda.

**Parámetros:**

* `search_keys`: número de CIK (p. ej., `320193` o `0000320193`)
* `search_keys_type`: `cik`

**Ejemplo:**

```
GET /api/v2/logos/search?search_keys=320193&search_keys_type=cik&fields=logo_light,logo_dark
```

**Ejemplo con CIK rellenado con ceros:**

```
GET /api/v2/logos/search?search_keys=0000320193&search_keys_type=cik&fields=logo_light,logo_dark
```

<div id="3-filter-by-isin">
  ### 3. Filtrar por ISIN
</div>

Busque usando el número internacional de identificación de valores.

**Parámetros:**

* `search_keys`: código ISIN (p. ej., `US0378331005`)
* `search_keys_type`: `isin`

**Ejemplo:**

```
GET /api/v2/logos/search?search_keys=US0378331005&search_keys_type=isin&fields=logo_light,logo_dark
```

<div id="4-filter-by-figi-share-class">
  ### 4. Filtrar por clase de acción FIGI
</div>

Busque usando el Financial Instrument Global Identifier de Bloomberg para clases de acciones.

**Parámetros:**

* `search_keys`: ID de la clase de acción FIGI (p. ej., `BBG001SQKGD7`)
* `search_keys_type`: `figi_share_class`

**Ejemplo:**

```
GET /api/v2/logos/search?search_keys=BBG001SQKGD7&search_keys_type=figi_share_class&fields=logo_light,logo_dark
```

<div id="5-filter-by-cusip">
  ### 5. Filtrar por CUSIP
</div>

Busque usando el número CUSIP (Committee on Uniform Securities Identification Procedures).

**Parámetros:**

* `search_keys`: número CUSIP (p. ej., `88160R101`)
* `search_keys_type`: `cusip`

**Ejemplo:**

```
GET /api/v2/logos/search?search_keys=88160R101&search_keys_type=cusip&fields=logo_light,logo_dark
```

<div id="6-filter-by-symbol-with-currency-country">
  ### 6. Filtrar por símbolo con moneda y país
</div>

Especifique los símbolos con códigos de país o moneda usando dos puntos (`:`) o punto (`.`) como delimitador.

**Parámetros:**

* `search_keys`: Símbolo con calificador (p. ej., `AAPL:US`, `AAPL:USD`, `AAPL.US`)
* `search_keys_type`: `symbol`

**Ejemplos:**

```
GET /api/v2/logos/search?search_keys=AAPL:US&search_keys_type=symbol&fields=logo_light,logo_dark
GET /api/v2/logos/search?search_keys=AAPL:USD&search_keys_type=symbol&fields=logo_light,logo_dark
GET /api/v2/logos/search?search_keys=AAPL.US&search_keys_type=symbol&fields=logo_light,logo_dark
```

<div id="7-filter-by-exchange-and-symbol">
  ### 7. Filtrar por bolsa y símbolo
</div>

Busque combinando el código de la bolsa con el símbolo. Admite formatos de estilo Bloomberg (heredado) y de estilo LSEG.

**Mapeo de estilo Bloomberg (heredado):**

* `search_keys`: Bolsa y símbolo (p. ej., `NASDAQ:AAPL`)
* `search_keys_type`: `symbol`

**Código LSEG actual de 3 caracteres:**

* `search_keys`: Código de bolsa y símbolo (p. ej., `NSQ:AAPL`)
* `search_keys_type`: `symbol`

**Ejemplos:**

```
GET /api/v2/logos/search?search_keys=NASDAQ:AAPL&search_keys_type=symbol&fields=logo_light,logo_dark
GET /api/v2/logos/search?search_keys=NSQ:AAPL&search_keys_type=symbol&fields=logo_light,logo_dark
```

<div id="8-filter-by-iso-mic-and-symbol">
  ### 8. Filtrar por ISO MIC y símbolo
</div>

Busque por código ISO MIC combinado con el símbolo.

**Parámetros:**

* `search_keys`: código MIC y símbolo (p. ej., `XYNS:AAPL`)
* `search_keys_type`: `symbol`

**Ejemplo:**

```
GET /api/v2/logos/search?search_keys=XYNS:AAPL&search_keys_type=symbol&fields=logo_light,logo_dark
```

<div id="9-crypto-logo-filters">
  ### 9. Filtros de logotipos de criptomonedas
</div>

Busque logotipos de criptomonedas en varios formatos. La API reconoce automáticamente los formatos de criptomonedas incluso cuando no se especifica `search_keys_type`.

**Formatos admitidos:**

* `CRYPTO:BTC`
* `CRYPTO/BTC`
* `$BTC`
* `BTC/USD`

**Ejemplos:**

```
GET /api/v2/logos/search?search_keys=CRYPTO:BTC&fields=logo_light,logo_dark
GET /api/v2/logos/search?search_keys=CRYPTO/BTC&fields=logo_light,logo_dark
GET /api/v2/logos/search?search_keys=$BTC&fields=logo_light,logo_dark
GET /api/v2/logos/search?search_keys=BTC/USD&fields=logo_light,logo_dark
```

<div id="available-fields">
  ## Campos disponibles
</div>

Puedes solicitar tipos específicos de logotipo mediante el parámetro `fields`. Los campos disponibles incluyen:

* `logo_light` - Logotipo para tema claro (PNG)
* `logo_dark` - Logotipo para tema oscuro (PNG)
* `logo_vector_light` - Logotipo vectorial para tema claro (SVG)
* `logo_vector_dark` - Logotipo vectorial para tema oscuro (SVG)
* `mark_light` - Marca/icono para tema claro (PNG)
* `mark_dark` - Marca/icono para tema oscuro (PNG)
* `mark_vector_light` - Marca vectorial para tema claro (SVG)
* `mark_vector_dark` - Marca vectorial para tema oscuro (SVG)
* `mark_composite_light` - Marca compuesta para tema claro (PNG)
* `mark_composite_dark` - Marca compuesta para tema oscuro (PNG)
* `mark_vector_composite_light` - Marca compuesta vectorial para tema claro (SVG)
* `mark_vector_composite_dark` - Marca compuesta vectorial para tema oscuro (SVG)

**Ejemplo de solicitud de varios campos:**

```
GET /api/v2/logos/search?search_keys=TSLA&fields=logo_light,logo_dark,mark_vector_composite_light
```

<div id="additional-parameters">
  ## Parámetros adicionales
</div>

<div id="image-customization">
  ### Personalización de imágenes
</div>

* `scale` - Escala de la imagen (p. ej., `100x100`, `300x300`)
* `max_width` - Ancho máximo de la imagen devuelta
* `composite_radius` - Radio del borde de las imágenes compuestas (entero, rango `0-50`)
* `composite_auto` - Genera automáticamente imágenes compuestas (booleano)

<div id="pagination-bulk-sync-only">
  ### Paginación (solo para bulk-sync)
</div>

* `page` - Número de página (entero)
* `pagesize` - Número de resultados por página (entero)
* `updated_since` - Filtrar por la marca de tiempo de actualización (formato ISO 8601)

<div id="security-details">
  ### Detalles de los valores
</div>

* `securities` - Incluye información detallada de los valores en la respuesta (booleano)

<div id="response-examples">
  ## Ejemplos de respuesta
</div>

<div id="200-success-response">
  ### Respuesta 200 exitosa
</div>

```json
{
  "ok": true,
  "data": [
    {
      "id": "efc3943a-ddac-4f59-a2cc-47a67583068b",
      "search_key": "TSLA",
      "securities": [
        {
          "symbol": "TSLA",
          "name": "TESLA INC",
          "cik": "1318605",
          "exchange": "NASDAQ",
          "mic_code": "XNAS",
          "exchange_name": "NASDAQ Global Select Consolidated",
          "cusip": "88160R101",
          "isin": "US88160R1014",
          "country": "US",
          "figi_share_class": "BBG001SQKGD7",
          "figi": "BBG000N9MNX3",
          "security_type": "Common Stock"
        }
      ],
      "files": {
        "logo_dark": "https://image-util.benzinga.com/api/v2/logos/file/image/1318605/logo_dark__53646042d4c8b507c7eddb70110ee334.png?x-bz-cred=...",
        "logo_light": "https://image-util.benzinga.com/api/v2/logos/file/image/1318605/logo_light__6895726d0ff148e46c238f91d033e72a.png?x-bz-cred=..."
      },
      "created_at": "2022-05-18T05:19:45.008547Z",
      "updated_at": "2025-02-05T09:43:14.303261Z"
    }
  ]
}
```

<div id="401-unauthorized-response">
  ### Respuesta 401 no autorizada
</div>

```json
{
  "ok": false,
  "errors": [
    {
      "code": "auth_failed",
      "id": "unauthorized",
      "value": "Invalid or missing authentication token"
    }
  ]
}
```

<div id="404-not-found-response">
  ### Respuesta 404 No encontrada
</div>

```json
{
  "ok": false,
  "errors": [
    {
      "code": "no_data_found",
      "id": "not_found",
      "value": "No logos found for the specified search key"
    }
  ]
}
```

<div id="500-internal-server-error-response">
  ### Respuesta de error interno del servidor 500
</div>

```json
{
  "ok": false,
  "errors": [
    {
      "code": "internal_server_error",
      "id": "server_error",
      "value": "An unexpected error occurred while processing your request"
    }
  ]
}
```

<div id="authentication">
  ## Autenticación
</div>

Todos los puntos finales de la API de Logos requieren autenticación mediante una clave de API. Incluya su token como parámetro de consulta:

```
?token=YOUR_API_KEY
```

<div id="best-practices">
  ## Mejores prácticas
</div>

1. **Especifique search&#95;keys&#95;type** - Aunque la API usa `symbol` de forma predeterminada, especificar el tipo explícitamente mejora la claridad y el rendimiento
2. **Solicite solo los campos necesarios** - Solicite únicamente los tipos de logotipo que necesite para minimizar el tamaño de la respuesta y mejorar el rendimiento
3. **Use bulk-sync para conjuntos de datos grandes** - Al recuperar varios logotipos, use el punto final `/logos/bulk-sync` con paginación
4. **Almacene en caché las respuestas** - Las URL de los logotipos incluyen marcas de tiempo de expiración; use la caché de forma adecuada para reducir las llamadas a la API
5. **Gestione los errores correctamente** - Compruebe siempre el campo `ok` en las respuestas y gestione adecuadamente los casos de error

<div id="rate-limits">
  ## Límites de velocidad
</div>

Consulta los detalles de los límites de velocidad en tu plan de suscripción de la API. Ponte en contacto con el equipo de soporte si necesitas límites de velocidad más altos para tu aplicación.

<div id="support">
  ## Soporte
</div>

Para obtener ayuda adicional o si tienes preguntas sobre la API de Logos, ponte en contacto con el soporte de la API de Benzinga o consulta la documentación detallada del punto final.