---
title: "Webhook配信のテスト"
openapi: /ja/openapi/webhook_api.spec.yml GET /api/v1/webhook/test
---

<div id="overview">
  ## 概要
</div>

このエンドポイントを使用すると、設定済みのエンドポイントにテスト用の webhook 配信をトリガーできます。これにより、本番環境のライブデータを受信する前に、webhook 統合が正しく機能していることを確認できます。

<div id="testing-your-integration">
  ## 統合のテスト
</div>

このエンドポイントを呼び出すと、Benzinga は設定した `destination` URL にテスト用の webhook ペイロードを送信します。このテスト配信では、本番環境の webhook 配信と同じ形式および再試行ロジックが使用されます。

<div id="what-to-expect">
  ### 想定される内容
</div>

1. **即時レスポンス**: テスト配信が正常にトリガーされると、API は `200` ステータスコードを返します
2. **テストペイロード**: webhook エンドポイントは、本番データと同じ形式のテストペイロードを受信します
3. **配信ヘッダー**: テスト配信には、本番配信と同様に `X-BZ-Delivery` ヘッダーが含まれます

<div id="verify-your-integration">
  ### 統合を検証する
</div>

このエンドポイントを使用して、次の点を確認できます。

* webhook エンドポイントにパブリックにアクセスできること
* エンドポイントが webhook のペイロード形式を正しく解析できること
* エンドポイントが適切なステータスコードで応答すること (成功時は 2xx) 
* エンドポイントが 30 秒のタイムアウト以内に応答すること
* 冪等性ロジックが `X-BZ-Delivery` ヘッダーとペイロードの `id` フィールドを正しく処理できること

<div id="best-practices">
  ## ベストプラクティス
</div>

* まずは本番環境ではない webhook エンドポイントでテストする
* エンドポイントが `200` または `204` のステータスコードを返すことを確認する
* ログと監視でテスト配信を捕捉できていることを確認する
* 重複排除ロジックがテスト配信 ID で正しく機能することを確認する
* 一時的にエラーのステータスコードを返して、エラーシナリオをテストする

<div id="troubleshooting">
  ## トラブルシューティング
</div>

<div id="424-delivery-error">
  ### 424 配信エラー
</div>

`424` ステータスコードを受け取った場合、システムはテスト用ペイロードを送信先エンドポイントに配信できませんでした。一般的な原因は次のとおりです。

* 送信先URLが公開アクセス可能ではない
* 送信先エンドポイントがエラーステータスコードを返している
* ネットワーク接続の問題
* 送信先エンドポイントのSSL/TLS証明書エラー

<div id="400-invalid-request">
  ### 400 無効なリクエスト
</div>

必須パラメータがすべて指定され、正しい形式になっていることを確認してください。

* `destination` は有効な HTTPS URL である必要があります
* `version` は `webhook/v1` である必要があります
* `kind` は `News/v1` である必要があります

<ResponseExample>
  ```json レスポンス (200 OK)
  {
    "status": "success"
  }
  ```

  ```json レスポンス（400 不正なリクエスト）
  {
    "error": "destination URL が無効です"
  }
  ```

  ```json レスポンス（424 依存関係の失敗）
  {
    "error": "テストペイロードを destination に配信できませんでした"
  }
  ```

  ```json レスポンス（401 認証されていません）
  {
    "ok": false,
    "errors": [
      {
        "code": "auth_failed",
        "id": "unauthorized",
        "value": "認証 token が無効か、指定されていません"
      }
    ]
  }
  ```

  ```json レスポンス（404 見つかりません）
  {
    "ok": false,
    "errors": [
      {
        "code": "no_data_found",
        "id": "not_found",
        "value": "指定されたパラメータに一致する data が見つかりません"
      }
    ]
  }
  ```

  ```json レスポンス（500 内部サーバーエラー）
  {
    "ok": false,
    "errors": [
      {
        "code": "internal_server_error",
        "id": "server_error",
        "value": "リクエストの処理中に予期しないエラーが発生しました"
      }
    ]
  }
  ```
</ResponseExample>