---
title: 'グローバル設定'
description: 'Mintlify では、docs.json ファイルを使用してドキュメントの外観や操作感を完全に制御できます'
icon: 'gear'
---

すべての Mintlify サイトには、基本設定を含む `docs.json` ファイルが必要です。詳細については、以下の[プロパティ](#properties)を参照してください。

<div id="properties">
  ## プロパティ
</div>

<ResponseField name="name" type="string" required>
  プロジェクト名です。グローバルタイトルに使用されます。

  例: `mintlify`
</ResponseField>

<ResponseField name="navigation" type="Navigation[]" required>
  各グループに含まれるすべてのページを持つグループの配列

  <Expandable title="ナビゲーション">
    <ResponseField name="group" type="string">
      グループ名です。

      例: `Settings`
    </ResponseField>

    <ResponseField name="pages" type="string[]">
      ページとして使用する markdown ファイルへの相対パスです。

      例: `["customization", "page"]`
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="logo" type="string or object">
  ロゴ画像へのパス、または &quot;light&quot; モードと &quot;dark&quot; モードのロゴ画像へのパスを含むオブジェクト

  <Expandable title="ロゴ">
    <ResponseField name="light" type="string">
      ライトモード用ロゴへのパス
    </ResponseField>

    <ResponseField name="dark" type="string">
      ダークモード用ロゴへのパス
    </ResponseField>

    <ResponseField name="href" type="string" default="/">
      ロゴをクリックしたときのリンク先
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="favicon" type="string">
  favicon 画像へのパス
</ResponseField>

<ResponseField name="colors" type="Colors">
  グローバルテーマ用の16進数カラーコード

  <Expandable title="色">
    <ResponseField name="primary" type="string" required>
      メインカラーです。ライトモードでは主に、強調表示されたコンテンツ、
      セクション見出し、アクセントに使用されます
    </ResponseField>

    <ResponseField name="light" type="string">
      ダークモード用のメインカラーです。ダークモードでは主に、強調表示された
      コンテンツ、セクション見出し、アクセントに使用されます
    </ResponseField>

    <ResponseField name="dark" type="string">
      重要なボタンに使用されるメインカラーです
    </ResponseField>

    <ResponseField name="background" type="object">
      ライトモードとダークモード両方の背景色

      <Expandable title="オブジェクト">
        <ResponseField name="light" type="string" required>
          ライトモードの背景の16進数カラーコード
        </ResponseField>

        <ResponseField name="dark" type="string" required>
          ダークモードの背景の16進数カラーコード
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="topbarLinks" type="TopbarLink[]">
  トップバーに含めるリンクの `name` と `url` の配列

  <Expandable title="トップバーリンク">
    <ResponseField name="name" type="string">
      ボタン名です。

      例: `Contact us`
    </ResponseField>

    <ResponseField name="url" type="string">
      ボタンをクリックしたときの URL です。例: `https://mintlify.com/contact`
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="topbarCtaButton" type="Call to Action">
  <Expandable title="トップバーの Call to Action">
    <ResponseField name="type" type={'"link" or "github"'} default="link">
      Link はボタンを表示します。GitHub は、指定した url のリポジトリ情報と GitHub スター数を表示します。
    </ResponseField>

    <ResponseField name="url" type="string">
      `link` の場合: ボタンのリンク先。

      `github` の場合: GitHub 情報の読み込み元となるリポジトリへのリンク。
    </ResponseField>

    <ResponseField name="name" type="string">
      ボタン内のテキストです。`type` が `link` の場合にのみ必要です。
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="versions" type="string[]">
  バージョン名の配列です。ナビゲーションバーのドロップダウンで異なるバージョン
  のドキュメントを表示する場合にのみ使用してください。
</ResponseField>

<ResponseField name="anchors" type="Anchor[]">
  アンカーの配列です。`icon`、`color`、`url` が含まれます。

  <Expandable title="アンカー">
    <ResponseField name="icon" type="string">
      アンカーを表すために使用する [Font Awesome](https://fontawesome.com/search?s=brands%2Cduotone) アイコンです。

      例: `comments`
    </ResponseField>

    <ResponseField name="name" type="string">
      アンカーラベルの名前です。

      例: `Community`
    </ResponseField>

    <ResponseField name="url" type="string">
      このアンカーに含めるページを判別する URL の先頭部分です。通常は、ページを配置するフォルダ名を指定します。
    </ResponseField>

    <ResponseField name="color" type="string">
      アンカーアイコンの背景色の 16 進カラーです。`from` と `to` にそれぞれ 16 進カラーを指定したオブジェクトを渡すと、グラデーションにすることもできます。
    </ResponseField>

    <ResponseField name="version" type="string">
      正しいドキュメントバージョンが選択されるまでアンカーを非表示にしたい場合に使用します。
    </ResponseField>

    <ResponseField name="isDefaultHidden" type="boolean" default="false">
      そのアンカー内のドキュメントへ直接リンクされるまで非表示にしたい場合は、`true` を渡します。
    </ResponseField>

    <ResponseField name="iconType" default="duotone" type="string">
      次のいずれか: &quot;brands&quot;, &quot;duotone&quot;, &quot;light&quot;, &quot;sharp-solid&quot;, &quot;solid&quot;, または &quot;thin&quot;
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="topAnchor" type="Object">
  最上位アンカーのデフォルト設定を上書きします。

  <Expandable title="オブジェクト">
    <ResponseField name="name" default="Documentation" type="string">
      最上位アンカーの名前です。
    </ResponseField>

    <ResponseField name="icon" default="book-open" type="string">
      Font Awesome アイコンです。
    </ResponseField>

    <ResponseField name="iconType" default="duotone" type="string">
      次のいずれか: &quot;brands&quot;, &quot;duotone&quot;, &quot;light&quot;, &quot;sharp-solid&quot;, &quot;solid&quot;, または &quot;thin&quot;
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tabs" type="Tabs[]">
  ナビゲーションタブの配列です。

  <Expandable title="Tabs">
    <ResponseField name="name" type="string">
      タブラベルの名前です。
    </ResponseField>

    <ResponseField name="url" type="string">
      このタブに含めるページを判別する URL の先頭部分です。通常は、
      ページを配置するフォルダ名を指定します。
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="api" type="API">
  API 設定の構成です。API ページの詳細については、[API Components](/ja/api-playground/demo) を参照してください。

  <Expandable title="API">
    <ResponseField name="baseUrl" type="string">
      すべての API エンドポイントのベース URL です。`baseUrl` が配列の場合は、ユーザーが切り替え可能な複数のベース URL
      オプションを有効にできます。
    </ResponseField>

    <ResponseField name="auth" type="Auth">
      <Expandable title="Auth">
        <ResponseField name="method" type="&#x22;bearer&#x22; | &#x22;basic&#x22; | &#x22;key&#x22;">
          すべての API エンドポイントで使用される認証方式です。
        </ResponseField>

        <ResponseField name="name" type="string">
          API プレイグラウンドで使用する認証パラメータ名です。

          method が `basic` の場合、形式は `[usernameName]:[passwordName]` である必要があります。
        </ResponseField>

        <ResponseField name="inputPrefix" type="string">
          認証入力フィールドの接頭辞として使用するためのデフォルト値です。

          例: `inputPrefix` に `AuthKey` を指定すると、認証フィールドのデフォルト入力値として `AuthKey` が使用されます。
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="playground" type="Playground">
      API プレイグラウンドの構成です

      <Expandable title="Playground">
        <ResponseField name="mode" default="show" type="&#x22;show&#x22; | &#x22;simple&#x22; | &#x22;hide&#x22;">
          プレイグラウンドを表示するか、非表示にするか、または追加のユーザー操作なしでエンドポイントのみを表示する `simple` を使用するかを指定します

          詳細は [playground guides](/ja/api-playground/demo) を参照してください
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="maintainOrder" type="boolean">
      このフラグを有効にすると、OpenAPI ページのキーの順序が OpenAPI ファイルで定義された順序と一致するようになります。

      <Warning>この動作はまもなくデフォルトで有効になり、その時点でこのフィールドは非推奨となります。</Warning>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="openapi" type="string | string[]">
  OpenAPI ファイルを指す URL または相対パスの文字列、
  あるいはそれらの文字列配列です。

  例:

  <CodeGroup>
    ```json Absolute
    "openapi": "https://example.com/openapi.json"
    ```

    ```json Relative
    "openapi": "/openapi.json"
    ```

    ```json Multiple
    "openapi": ["https://example.com/openapi1.json", "/openapi2.json", "/openapi3.json"]
    ```
  </CodeGroup>
</ResponseField>

<ResponseField name="footerSocials" type="FooterSocials">
  ソーシャルメディアアカウントを表すオブジェクトです。key:property の組は、ソーシャルメディアプラットフォームとアカウント URL を表します。

  例:

  ```json
  {
    "x": "https://x.com/mintlify",
    "website": "https://mintlify.com"
  }
  ```

  <Expandable title="FooterSocials">
    <ResponseField name="[key]" type="string">
      次のいずれかの値です: `website`, `facebook`, `x`, `discord`, `slack`, `github`, `linkedin`, `instagram`, `hacker-news`

      例: `x`
    </ResponseField>

    <ResponseField name="property" type="string">
      ソーシャルプラットフォームの URL です。

      例: `https://x.com/mintlify`
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="feedback" type="Feedback">
  フィードバックボタンを有効にするための構成です

  <Expandable title="Feedback">
    <ResponseField name="suggestEdit" type="boolean" default="false">
      ユーザーがプルリクエスト経由で編集を提案できるボタンを有効にします
    </ResponseField>

    <ResponseField name="raiseIssue" type="boolean" default="false">
      ユーザーがドキュメントに関する issue を報告できるボタンを有効にします
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="modeToggle" type="ModeToggle">
  ダークモードの切り替えをカスタマイズします。

  <Expandable title="ModeToggle">
    <ResponseField name="default" type={'"light" or "dark"'}>
      新規ユーザーに対して常にライトモードまたはダークモードを表示するかどうかを設定します。設定しない場合は、デフォルトでユーザーのオペレーティングシステムと同じモードが使用されます。
    </ResponseField>

    <ResponseField name="isHidden" type="boolean" default="false">
      ダーク/ライトモードの切り替えを非表示にするには、true に設定します。`isHidden` と `default` を組み合わせると、ドキュメントをライトモードまたはダークモードのみに固定できます。例:

      <CodeGroup>
        ```json ダークモードのみ
        "modeToggle": {
          "default": "dark",
          "isHidden": true
        }
        ```

        ```json ライトモードのみ
        "modeToggle": {
          "default": "light",
          "isHidden": true
        }
        ```
      </CodeGroup>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="backgroundImage" type="string">
  すべてのページの背後に表示される背景画像です。[Infisical](https://infisical.com/docs) および [FRPC](https://frpc.io) の例を参照してください。
</ResponseField>